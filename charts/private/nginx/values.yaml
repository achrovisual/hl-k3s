ingress-nginx:
  namespaceOverride: nginx
  controller:
    ingressClassResource:
      default: true
    ingressClass: nginx
    service:
      type: LoadBalancer
    admissionWebhooks:
      patch:
        nodeSelector:
          kubernetes.io/os: linux
  gateway:
    enabled: true