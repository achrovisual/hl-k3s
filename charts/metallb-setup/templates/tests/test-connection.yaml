apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "metallb-setup.fullname" . }}-test-connection"
  labels:
    {{- include "metallb-setup.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "metallb-setup.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
